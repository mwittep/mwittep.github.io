{{- /*
  Search engines and link-preview bots are welcome: they are what make the site
  findable and make shared links render a card.

  The AI-training crawlers below are opted out. This is a request, not a control
  -- it works only because those operators choose to honour robots.txt, and it
  does nothing about scrapers that ignore it. GitHub Pages serves static files,
  so no rule can actually be enforced here.

  Google-Extended is Gemini training only; it is a separate crawler from
  Googlebot, so opting out costs nothing in Search ranking.
*/ -}}
User-agent: *
Allow: /

# AI training crawlers (these honour robots.txt)
User-agent: GPTBot
User-agent: OAI-SearchBot
User-agent: ChatGPT-User
User-agent: ClaudeBot
User-agent: Claude-Web
User-agent: anthropic-ai
User-agent: CCBot
User-agent: Google-Extended
User-agent: Applebot-Extended
User-agent: PerplexityBot
User-agent: Bytespider
User-agent: Amazonbot
User-agent: meta-externalagent
User-agent: Diffbot
User-agent: Omgilibot
Disallow: /

Sitemap: {{ "sitemap.xml" | absURL }}
